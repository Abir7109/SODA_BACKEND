{
  "name": "soda-hud-android",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build:web": "cd .. && npm run build",
    "copy": "npx cap copy",
    "sync": "npx cap sync",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.1.0",
    "@capacitor/core": "^8.1.0",
    "@capacitor/local-notifications": "^8.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0"
  }
}
